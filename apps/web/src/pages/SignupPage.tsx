import { Link, Navigate } from "react-router-dom"; import { Card } from "../components/Card"; import { SignupForm } from "../features/auth/SignupForm"; import { useAuth } from "../state/auth/AuthContext"; import { useDocumentTitle } from "../hooks/useDocumentTitle";
export function SignupPage(): React.JSX.Element { useDocumentTitle("Create account"); const { state } = useAuth(); return state.status === "authenticated" ? <Navigate to="/dashboard" replace /> : <main className="page-shell"><Card><h1>Start tracking</h1><SignupForm /><p>Already have an account? <Link to="/login">Sign in</Link></p></Card></main>; }
