import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import { listChatMessages, sendChatMessage } from "../features/chat/api";
import type { ChatMessage } from "../api/contracts/chat";
export function ChatSessionPage(): React.JSX.Element { const { sessionId = "" } = useParams(); const [messages, setMessages] = useState<ChatMessage[]>([]); const [message, setMessage] = useState(""); useEffect(() => { void listChatMessages(sessionId).then(value => setMessages(value.data as ChatMessage[])).catch(() => setMessages([])); }, [sessionId]); async function submit(): Promise<void> { if (!message.trim()) return; const result = await sendChatMessage(sessionId, message, Intl.DateTimeFormat().resolvedOptions().timeZone); setMessages(items => [...items, result.data.assistantMessage as ChatMessage]); setMessage(""); } return <section><h1>Nutrition Assistant</h1><p>This assistant is not medical advice.</p><ol>{messages.filter(item => item.role !== "TOOL").map(item => <li key={item.id}><strong>{item.role}</strong>: {item.content}</li>)}</ol><textarea maxLength={2000} value={message} onChange={event => setMessage(event.target.value)} /><button onClick={() => void submit()}>Send</button></section>; }
