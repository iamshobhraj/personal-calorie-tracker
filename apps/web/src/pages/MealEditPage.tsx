import { useParams } from "react-router-dom"; import { useQuery } from "@tanstack/react-query"; import { getMeal } from "../features/meals/api"; import { LoadingState } from "../components/LoadingState"; import { MealForm } from "../features/meals/MealForm"; export function MealEditPage(): React.JSX.Element { const { mealEntryId } = useParams(); const query = useQuery({ queryKey: ["meal", mealEntryId], queryFn: () => getMeal(mealEntryId ?? "") }); return query.isLoading ? <LoadingState /> : query.data ? <><h1>Edit meal</h1><MealForm meal={query.data.data} /></> : <p>Meal not found.</p>; }
