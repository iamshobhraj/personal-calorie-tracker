import { Link } from "react-router-dom"; import type { MealEntry } from "../../api/contracts/meals"; import { EmptyState } from "../../components/EmptyState"; import { Table } from "../../components/Table";
export function MealTable({meals}:{meals:MealEntry[]}):React.JSX.Element{return meals.length===0?<EmptyState>No meals match these dates.</EmptyState>:<Table><thead><tr><th>Date</th><th>Meal</th><th>Food</th><th>Energy</th><th /></tr></thead><tbody>{meals.map(meal=><tr key={meal.id}><td>{meal.localDate}</td><td>{meal.mealType}</td><td>{meal.foodName}</td><td>{meal.nutrients.find(n=>n.code==="ENERGY_KCAL")?.amount??"Unknown"} kcal</td><td><Link to={`/meals/${meal.id}/edit`}>Edit</Link></td></tr>)}</tbody></Table>}
