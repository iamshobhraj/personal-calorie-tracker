import { Bar, BarChart, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { MacroPoint } from "../api/contracts/reports"; import { ChartFrame } from "./ChartFrame"; export function MacroChart({ data }: { data: MacroPoint[] }): React.JSX.Element { return <ChartFrame title="Macros (g)"><ResponsiveContainer width="100%" height={250}><BarChart data={data.map(p => ({ periodStart: p.periodStart, protein: p.totals.protein.amount, carbohydrate: p.totals.carbohydrate.amount, fat: p.totals.fat.amount }))}><XAxis dataKey="periodStart" /><YAxis /><Tooltip /><Legend /><Bar dataKey="protein" fill="#126b54" /><Bar dataKey="carbohydrate" fill="#e2a43b" /><Bar dataKey="fat" fill="#b75c48" /></BarChart></ResponsiveContainer></ChartFrame>; }
