import { ChartFrame } from "./ChartFrame"; import type { GoalComparisonPoint } from "../api/contracts/reports"; export function GoalComparisonChart({ data }: { data: GoalComparisonPoint[] }): React.JSX.Element { return <ChartFrame title="Goal comparison">{data.length ? <ul>{data.map(point => <li key={point.periodStart}>{point.periodStart}: {point.goalId ? `${point.comparisons.length} nutrient targets` : "No goal set"}</li>)}</ul> : <p>No goal data for this period.</p>}</ChartFrame>; }
