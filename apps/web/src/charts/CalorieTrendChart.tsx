import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { CalorieTrendPoint } from "../api/contracts/reports"; import { ChartFrame } from "./ChartFrame"; export function CalorieTrendChart({ data }: { data: CalorieTrendPoint[] }): React.JSX.Element { return <ChartFrame title="Calorie trend"><ResponsiveContainer width="100%" height={250}><LineChart data={data}><XAxis dataKey="periodStart" /><YAxis /><Tooltip /><Line dataKey="calories" stroke="#126b54" connectNulls={false} /></LineChart></ResponsiveContainer><p>Missing values are unknown, not zero.</p></ChartFrame>; }
