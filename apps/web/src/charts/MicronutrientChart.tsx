import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { MicronutrientSummary } from "../api/contracts/reports"; import { ChartFrame } from "./ChartFrame"; export function MicronutrientChart({ data }: { data: MicronutrientSummary[] }): React.JSX.Element { return <ChartFrame title="Micronutrients"><ResponsiveContainer width="100%" height={250}><BarChart data={data}><XAxis dataKey="nutrientCode" /><YAxis /><Tooltip /><Bar dataKey="amount" fill="#4176a8" /></BarChart></ResponsiveContainer><ul>{data.map(item => <li key={item.nutrientCode}>{item.name}: {item.coveragePercent}% coverage</li>)}</ul></ChartFrame>; }
