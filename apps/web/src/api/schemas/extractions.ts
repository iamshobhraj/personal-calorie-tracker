import { z } from "zod";
import { nutrientCodeSchema } from "./resources";
export const extractionSchema = z.object({ id: z.string().uuid(), status: z.enum(["PROCESSING", "SUCCEEDED", "FAILED"]), result: z.object({ imageKind: z.enum(["LABEL", "PLATE"]), foodName: z.string(), quantity: z.object({ value: z.number(), unit: z.string(), description: z.string().nullable() }), suggestedMealType: z.enum(["BREAKFAST", "LUNCH", "DINNER", "SNACKS"]).nullable(), nutrients: z.array(z.object({ code: nutrientCodeSchema, amount: z.number(), unit: z.enum(["kcal", "g", "mg", "mcg"]), confidence: z.number(), provenance: z.enum(["LABEL_AI", "PLATE_AI"]) })), overallConfidence: z.number(), warnings: z.array(z.string()), requiresUserConfirmation: z.literal(true) }).nullable(), failure: z.object({ code: z.string(), message: z.string() }).nullable() });
